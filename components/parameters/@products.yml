get:
  - in: query
    name: page
    schema:
      type: integer
  - in: query
    name: pageSize
    schema:
      type: integer
  - in: query
    name: query
    schema:
      type: string
  - in: query
    name: sortField
    schema:
      type: string
  - in: query
    name: brandId
    schema:
      type: string
  - in: query
    name: attributeSetId
    schema:
      type: string
  - in: query
    name: productLineId
    schema:
      type: string
  - in: query
    name: categoryId
    schema:
      type: string
  - in: query
    name: sellingStatus
    schema:
      type: string
  - in: query
    name: channelId
    schema:
      type: string
  - in: query
    name: objective
    schema:
      type: string
  - in: query
    name: sortOrder
    schema:
      type: string
  - in: query
    name: isBundle
    schema:
      type: boolean
