get:
  - in: query
    name: page
    schema:
      type: integer
      example: 1
  - in: query
    name: pageSize
    schema:
      type: integer
      example: 10
  - in: query
    name: query
    schema:
      type: string
