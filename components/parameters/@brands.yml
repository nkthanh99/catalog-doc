get:
  - in: query
    name: query
    schema:
      type: string
  - in: query
    name: is_active
    schema:
      type: integer
      enum: [0, 1]
    example: 0
  - in: query
    name: approvedStatus
    schema:
      type: integer
      enum: [0, 1]
    example: 0
  - in: query
    name: pageSize
    schema:
      type: integer
    example: 10
  - in: query
    name: page
    schema:
      type: integer
    example: 1
