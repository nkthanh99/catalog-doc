post:
  content:
    application/json:
      schema:
        allOf:
          - $ref: ../schemas/GenericProduct.yml
          - type: object
            properties:
              code:
                type: string
