InternalServer:
  description: Server error
  content:
    application/json:
      schema:
        type: object
        properties:
          code:
            type: string
            default: ERROR
          message:
            type: string
