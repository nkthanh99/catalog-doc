description: Client request invalid
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: string
          default: INVALID
        message:
          type: string
        errors:
          type: object  